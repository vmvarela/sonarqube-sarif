# Agent tooling (not part of the project)
AGENTS.md
skills-lock.json
.agents/

node_modules/
lib/
*.log
.DS_Store
.env
.idea/
.vscode/
*.sarif
coverage/
.act
.secrets
scripts/

# Keep only dist/index.js (the bundled action entry point).
# tsc emits individual .js files into dist/ but only the esbuild bundle matters.
dist/*
!dist/index.js
